<div class="col-sm-6">
        <ol class="breadcrumb float-sm-right">
          <li class="breadcrumb-item"><a href="{{ route($url) }}">{{ isset($name) ? $name : '' }}</a></li>
          <li class="breadcrumb-item active">{{ isset($key) ? $key : '' }}</li>
        </ol>
 </div>